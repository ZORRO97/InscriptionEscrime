{% extends 'SheadFencingBundle::layout.html.twig' %}

{% block content %}
    {# total items count #}
<div class="count">
    <h1>{{ pagination.getTotalItemCount }} Arbitres</h1>
</div>
<table class="table">
<tr>
{# sorting of properties based on query components #}
 {#   <th>{{ knp_pagination_sortable(pagination, 'Id', 'a.id') }}</th>
    <th{% if pagination.isSorted('a.code') %} class="sorted"{% endif %}>{{ knp_pagination_sortable(pagination, 'Code', 'a.code') }}</th> #}
    <th>NOM</th>
    <th>PRENOM</th>
    <th>N°licence</th>
    <th>CLUB</th>
    <th>EPEE</th>
    <th>FLEURET</th>
    <th>SABRE</th>
</tr>

{# table body #}
{% for article in pagination %}
<tr {% if loop.index is odd %}class="color"{% endif %}>
    <td>{{ article.licencie.nom }}</td>
    <td>{{ article.licencie.prenom }}</td>
    <td>{{ article.licencie.codet }}</td>
    <td> {{ article.licencie.club.nom }} </td>

    <td>{{ article.arbE }}</td>
    <td>{{ article.arbF }}</td>
    <td>{{ article.arbS }}</td>

</tr>
{% endfor %}
</table>
{# display navigation #}
<div class="navigation">
    {{ knp_pagination_render(pagination) }}
</div>
{% endblock %}